# Icon attribution

Fluxo packages a small, local icon set so the browser extension can run without CDN access.

- UI glyphs are sourced from `lucide-static` under the ISC license.
- Network marks are sourced from `simple-icons`; brand marks remain trademarks of their owners.

Only icons used by the extension popup are vendored.
